apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-container
spec:
  replicas: 3
  selector:
    matchLabels:
      app: gateway-container
  template:
    metadata:
      labels:
        app: gateway-container
    spec:
      containers:
      - name: gateway-container
        image: maalami/springbootgateway
        ports:
        - containerPort: 8442
      nodeSelector:
        role: gateway
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-service
spec:
  selector:
    app: gateway-container
  ports:
    - port: 8442
      targetPort: 8442
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gateway-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: ventaonline.tfg.etsii.urjc.es
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gateway-service
            port:
              number: 8442
